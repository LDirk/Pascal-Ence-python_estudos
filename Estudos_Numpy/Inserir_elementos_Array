import numpy as np
arr = np.array([1,2,3])
print(arr)

#Inserindo elemento, no exemplo 1 e 10
a = np.insert(arr, 1,10)
print(a)

b = np.array([[1,2],[3,4]])
print(b)

#Somando as colunas da matriz b
print(b.sum(axis = 0))

#Somando as linhas
print(b.sum(axis = 1))

